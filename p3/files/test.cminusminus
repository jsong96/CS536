int x;
bool y;
void z;
struct s t;
void foo() {}
struct s {int x;int y;int z;};
int foo (int a, int b) {if (b == -1) { x = 4+3*5-y; while (c) { y = y*2+x; } } else { x = 0; }}

int bar(int a) { if (a < 2) {a++; a--;} a = a + 3; a = b / 3; c = b * 2 * 3 + 3; return a;}

void readAndWrite() { cin >> reader.token; cout << reader.token; cout << "success";}

int testTerminalString() {
    a = 0;
    y = true;
    y = false;
    if (!a) { a = 2;}
    if (s.x + 2 > 1 || b == 0) {}
    while (a < 3) {cout << "small!!!\t"; a++;}
    repeat (a == 3) {}
    if (!y && a < 3) {a = 1;}
    str = "this string terminates correctly \t \"\\\n";
    return str;
}